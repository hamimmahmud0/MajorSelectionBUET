# ─── Flask Core ───
SECRET_KEY=buet-civil-major-selection-secret-key-2026
FLASK_ENV=development

# ─── Admin Credentials ───
ADMIN_USERNAME=hamim_mahmud
ADMIN_PASSWORD=majorminor#@

# ─── Database ───
# Relative path (dev):  sqlite:///major_selection.db
# Absolute path (prod): sqlite:////home/hamim-mahmud/Workspace/MajorSelectionBUET/instance/major_selection.db
DATABASE_URI=sqlite:///major_selection.db

# ─── Production Mode ───
# Set to "true" when deploying with gunicorn/uvicorn
PRODUCTION=false

# ─── Student ID Validation ───
# First 4 digits that every valid student ID must start with
STUDENT_ID_PREFIX=2104

# ─── Production Overrides (uncomment for production) ───
# FLASK_ENV=production
# SERVER_NAME=your-domain.com
# PORT=8000
